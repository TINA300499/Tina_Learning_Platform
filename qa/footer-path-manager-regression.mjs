import fs from"node:fs";import path from"path";import{fileURLToPath}from"url";const R=path.resolve(path.dirname(fileURLToPath(import.meta.url)),".."),s=fs.readFileSync(path.join(R,"workspace-completion-v14.js"),"utf8"),r=[],t=(n,p)=>r.push({name:n,pass:!!p});t("route-options",s.includes("const FOOTER_ROUTE_OPTIONS="));t("dictionary-route-listed",s.includes('["Tina Dictionary","dictionary-extra"]'));t("quick-destination-picker",s.includes("data-footer-route-picker"));t("editable-destination",s.includes("Destination / path"));t("route-validation",s.includes("function footerRouteExists(target)"));t("external-url-validation",s.includes('["http:","https:"]'));t("test-path-button",s.includes("data-footer-test"));t("validate-all",s.includes('id="footerValidateAll"'));t("invalid-save-blocked",s.includes("Footer not saved. Fix invalid destinations first."));t("audit-on-save",s.includes('auditEvent("footer.config.updated"'));const f=r.filter(x=>!x.pass);console.log(JSON.stringify({suite:"footer-path-manager",passed:r.length-f.length,failed:f.length,results:r},null,2));if(f.length)process.exitCode=1;